# Next-session handoff

_Written: 2026-06-20 16:02 PT_

## Resume prompt
> "Loop-engineering: Phase-1 (#37) + **ADR-020 per-session loop-state** (#38) are merged and dogfooded green (93/0). Pick next: (a) **ADR-021** — superpowers design-before-code gate, (b) define the **ultracode signal** (still zero hits; blocks ADR-019 ceiling-lift + ADR-021), or (c) **Phase 2** (Supabase `loop_runs` + live mid-flight cost monitor)."

## Branch & state
- Branch: `main` (worktree: N/A) — up to date with origin, clean of session work.
- Pre-existing uncommitted (NOT this session, left untouched): `.claude/stack-config.json`, `hooks/session-start-handoff.sh`, `skills/project-init/SKILL.md`.
- No open PRs.

## What shipped this session
- `85a3341` — feat(loop-eng): per-session loop-state (ADR-020) — PR #38 (squash-merged).
- Includes ADR-020 doc (`docs/ADRs/020-per-session-loop-state.md`) + the fix across `loop_lib.sh`, `hooks/loop-stop.sh`, `skills/loop-engineer/SKILL.md`, and 6 new isolation tests. Suite `tests/test-loop-engineering.sh` **93/0**.
- Local `~/.claude` updated via `./scripts/update.sh --tier=4` (fix is installed, not just in-repo).

## What's blocked & why
- Nothing blocked. ADR-021 / ultracode / Phase-2 are new work.

## Exact next steps
1. **ADR-021 — superpowers design-before-code gate.** (Was the original ADR-020 slot; renumbered when the loop-state fix took 020.) Vendor `using-superpowers` (Tier 0/1) + brainstorming's text core (drop the visual-companion Node server); a PreToolUse `Edit|Write` `permissionDecision: deny`-until-spec-exists hook (reuse ADR-019's governance-hooks base); reconcile with the existing `/plan` skill (replace / wrap / mode).
2. **Define the ultracode signal** — grep still returns zero hits. A config flag / env the stack can read; needed by ADR-019's autonomy ceiling-lift AND the ADR-021 gate. Decide where it lives (stack-config? settings.json? session-state?).
3. **Phase 2** — Supabase `loop_runs` table (Tier 3+), live mid-flight cost monitor (closes the within-iteration runaway gap; Phase-1 cost cap is advisory/between-iteration), per-iteration telemetry to `subagent-runs.jsonl`. **data-engineer should fire here** (schema migration).

## Gotchas
- **Dogfood / live loops need a session with no user chatter** — a user reply preempts the Stop-hook; the iteration counter only advances when a turn ends with NO input. To drive a loop to its cap, stay silent.
- **Cap-trip emits no block text** — absence of a `decision:block` message IS the terminating signal; only `status` flips to `max_iterations`.
- **Loop-state is now per-session** (ADR-020): keyed by `CLAUDE_CODE_SESSION_ID` → `loop-state.<sid>.json`. Legacy single-file fallback when no sid. Tests neutralize a leaked parent session id.
- **macOS pty exhaustion bit us this session** — `kern.tty.ptmx_max=511` with 527 nodes → `forkpty: Device not configured` (all terminals dead until reboot). Unrelated to repo work. If it recurs, reboot or raise the cap permanently (`kern.tty.ptmx_max=2048` in `/etc/sysctl.conf`).
- **`irreversible-deny` is best-effort defense-in-depth, NOT a security boundary.** Don't chase bypass-proofing.
- **Gemini CLI still tier-blocked** — route adversarial review via Codex.
- Sandbox CAN push branches + open/merge PRs (confirmed this session: #38 pushed + squash-merged).

## Cross-repo references
- None. Stack source repo dogfoods itself (Tier 4).

## Team this session
- Used: none dispatched — work was author-direct (ADR + implementation + tests). Dogfood Tests 1–2 ran in a separate fresh Claude session, not via subagents.
- Benched (should-have-fired):
  - **reviewer (Codex):** ADR-020 fix merged without an adversarial cross-model pass. The loop-eng memory says keep cross-model review in the loop for this subsystem — run a Codex review on `85a3341` retroactively, or before ADR-021 code lands.
  - **architect:** no architect dispatch before the code change (mitigated: ADR-020 served as the design record, MCQ-confirmed before code).
- Scratch (gitignored, on this machine): `.claude/scratch/dogfood-findings.md`, `dogfood-loop-engineer.md`, `resume-after-reboot.md`.
