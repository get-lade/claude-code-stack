# Next-session handoff

_Written: 2026-07-05 08:05 PT_

## Branch & state
- Branch: `main` @ `e13c142` (worktree: N/A)
- Uncommitted: clean (before this handoff commit)
- Behind/ahead of origin: up to date

## What shipped this session
- `e13c142` — **#73 ADR-033**: post-session **model-fit receipt**. At session end the stack tells you whether the model you used fit the work you did, and estimates what a cheaper/stronger tier would've cost. Complements `/model-audit` (freshness) with a *fit* check. `stack_version` 1.2.0 → 1.3.0. CI green, squash-merged.

### What it is (for next session's context)
- New Stop hook `hooks/model-fit-turn.sh` logs one `main_turn` row/turn to `subagent-runs.jsonl` (`event="main_turn"`, `agent="main"`).
- New lib fn `model_fit_receipt_line` (`skills/loop-engineer/loop_lib.sh`) reads them at session end → ratio `gen_per_edit = out_tokens / max(edit_calls,1)`. Ratio-based on purpose: input-context size cancels out, killing the "every turn pulls 40k → always looks like reasoning" fail-open the critic caught.
- Bands (v1-tunable, live in the lib not config): `<300 & edit_calls≥10` = mechanical → suggest cheaper; `>1200` = reasoning → keep/step up; else no advice. Min-evidence gate ≥6 turns.
- Surfaced in `/handoff`, `/goodmorning`, + a once-per-session Stop-hook fallback line.
- New pref `session_prefs.model_fit_receipt` (`off|on`, default `on`), threaded through the ADR-017 parity contract.

## What's blocked & why
- Nothing blocked. `main` green, PR #73 merged.

## Exact next steps
1. **Tune the v1 bands from real logs.** The 300/1200 `gen_per_edit` thresholds are guesses (ADR-033 Open Questions #1). After a few sessions accumulate `main_turn` rows, sanity-check whether real mechanical vs reasoning sessions actually split at those numbers; adjust in `model_fit_receipt_line`.
2. **Optional — decide the Stop-hook fallback's fate** (ADR-033 Open Q #3): ship kept both `/handoff` receipt + the once-per-session fallback. If telemetry shows `/handoff` coverage is high, drop the fallback to simplify.
3. **Optional cleanup:** the pre-existing `BASH_SOURCE`/`:-` bash-3.2 quirk in `loop_cost_from_usage` (flagged by implementer, left untouched as out-of-scope). Fix separately if bash 3.2 support matters.

## Gotchas
- **`model` from the Stop payload is untrusted.** ADR-033 sanitizes it to `[A-Za-z0-9._-]` at write/read/print because a crafted model string could break out of `<system-reminder>` and emit a literal `{"decision":"block"}` (reviewer-caught, now covered by a regression test). If you touch the hook or the receipt lib, keep all three sanitize layers.
- **New `main_turn` event in `subagent-runs.jsonl`.** Any NEW reader of that log MUST filter by event — `main_turn` rows carry `agent:"main"` and will be miscounted as a subagent named "main" otherwise. Reviewer found `agent-performance-review` had this bug; it's fixed, but the trap is live for future readers.
- **`session-start.txt` timing.** The `/handoff` team-utilization jq filters `.ts >= session_start`, but the marker updated to 14:57Z (after the session's dispatches), so the time-filtered query returned empty. Real team came from the `complete` rows (dispatch rows log `agent:"unknown"`).
- Sandbox CAN push branches, open PRs, and merge here (did all three this session) — the older "can't merge" note did not apply with an explicit human "merge it".

## Team this session
- Used: architect ×1 (+2 resumes: revise-to-receipt, redesign), architecture-critic ×1 (Gemini via API — caught the 40k fail-open + subagent-contamination blockers), implementer ×1 (+1 resume for reviewer fixes), reviewer ×1 (Codex via OpenAI API — caught the model-string injection + missed reader).
- Benched (should-have-fired):
  - **product-critic**: the "non-trivial feature → product-critic before architect" rule flags a net-new user-facing feature. Skipped; went straight to architect. Low regret (the architecture-critic's ROI challenge covered "is this worth building"), but noting the rule.
  - **validator**: "implementer claims done → validator runs real data and asserts values" rule. Relied on the implementer's own 49-assertion suite + `verify --tier=2` instead of a separate validator pass. Acceptable here (pure hook/lib logic, exhaustive unit fixtures incl. adversarial cases), but the roster rule flags it.
