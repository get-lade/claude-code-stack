# Next-session handoff

_Written: 2026-07-01 09:38 PT_

## Branch & state
- Branch: `main` (worktree: N/A)
- Uncommitted: clean (handoff files only)
- Behind/ahead of origin: up to date

## What shipped this session
- `335f21c` — feat(standards): add Karpathy's six loop-era rules (ten-rule set) ([#64](https://github.com/bschonbrun/claude-code-stack/pull/64))
- `0f1cc1a` — chore(release): bump stack_version 1.1.3 → 1.1.4 ([#65](https://github.com/bschonbrun/claude-code-stack/pull/65))

Extended the global CLAUDE.md standards with Karpathy's **six loop-era rules** (the "ten-rule" set) on top of the four originals:

1. Added an always-active **Loop & Self-Check Discipline** section (rules 5–10) to `config/claude.md.global.template` — Verification, Goal-driven, Debugging, Dependencies, Communication, Named failure modes (Kitchen Sink / Wrong Abstraction / Optimistic Path / Runaway Refactor).
2. `skills/loop-engineer/SKILL.md` step 6 now references rules 5–10 as the **per-iteration self-check gate** (placed here, not `loop-review`, which is telemetry/cap-tuning only).
3. Propagated the same section into the live `~/.claude/CLAUDE.md` (active immediately on this machine).
4. Bumped `stack_version` 1.1.3 → 1.1.4 (`.claude/stack-config.json`, `templates/stack-defaults.template.json`, regenerated `config/capability-registry.json`) + CHANGELOG entry.

Both PRs green + squash-merged to main. Placement decided with the user via MCQ ("Both": global template as source of truth + loop layer references it).

## What's blocked & why
- Nothing blocked. main green, no open PRs.

## Exact next steps
1. **Other machines**: run `update.sh` (or `/stack-refresh`) to pull rules 5–10 — `/goodmorning` step 6c will nudge them ("N behind"). On re-sync, `append_stack_section` replaces the `<!-- CLAUDE_CODE_STACK_MANAGED -->` block.
2. **Open thread — deeper propagation design** (user wanted to discuss): whether the repo-root project CLAUDE.md (`templates/PROJECT-CLAUDE.md.template`) should also embed/reference the ten rules, or stay global-only; force-resync vs. nudge; any cross-machine announce channel.
3. Optional: `/model-audit` still due (last 2026-05-16, 46+ days) — unrelated to this work but surfaced at boot.

## Gotchas
- Cloud propagation is already automatic: `cloud-bootstrap.sh` clones main + runs `install.sh` fresh per ephemeral session — no plugins involved (the "cloud has no plugins" limit doesn't apply). The merge to main was sufficient.
- Stack freshness detection is **SHA-based** (`source_sha` in `~/.claude/.stack-install.json` vs repo HEAD), NOT version-number-based — so the `/goodmorning` nudge already fires on any new main commit; the 1.1.4 bump is a semantic/changelog signal, not the trigger.
- `append_stack_section` REPLACES between the `CLAUDE_CODE_STACK_MANAGED` markers on re-run (not append-if-absent), so existing installs cleanly update. Live `~/.claude/CLAUDE.md` was hand-edited but matches the template, so re-sync is a no-op there.
- `capability-registry.json` is generated — run `scripts/gen-capability-registry.sh` after any skill/agent/stack_version change or CI lint fails. Done this session.
- Sandbox note: this session was authorized to push to main + merge PRs directly; that is NOT the default (normally branch + PR + hand off).

## Team this session
- Main thread only — no subagents dispatched. Docs/config change; no architect/reviewer routing warranted.
