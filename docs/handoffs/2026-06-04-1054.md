# Next-session handoff

_Written: 2026-06-04 10:54 PT_

## ⚡ START HERE — final all-agents re-test (close F2)

F2 (custom subagents hallucinate tool calls) is **root-caused + fixed + merged**
to `main` (#22). Root cause: `agents/*.md` declared `tools:` as a lowercase YAML
list; Claude Code tool names are PascalCase, so the toolset resolved empty and
agents emitted `<invoke …>` as literal text. Fix: all 14 agents now use CSV
PascalCase (`tools: Read, Grep, Glob, Bash`), synced to `~/.claude/agents/`.

`designer` was verified PASS on a fresh boot (it was the test vector). The other
**13 agents are fixed + synced but were cached stale** in the verifying session —
they are **NOT yet re-tested on a fresh boot**. That is the only open work.

### Re-test (must be a FRESH 1M Desktop boot — agent defs cache at session start)
1. Confirm parent model id is `claude-opus-4-8[1m]`.
2. Dispatch 2–3 of the fixed agents (Agent tool, NO `model:` override), e.g.
   `reviewer`, `implementer`, `validator`, with a real Read smoke test:
   > Read `agents/<name>.md` and report line 1, total line count, and the
   > `model:` value. Return only those three facts.
3. Ground-truth each with Bash (`head -1`, `wc -l`, `grep '^model:'`).
4. PASS = `tool_uses >= 1` AND accurate facts (not hallucinated) for every agent.
   Then mark F2 fully closed in `docs/runbooks/stack-cloud-overrides.md`
   (currently says "FIXED" but pending the 13-agent confirm).
5. FAIL on any agent → re-open; capture transcript + parent model id.

## Branch & state
- Branch: `main` — up to date with origin, 0 ahead / 0 behind.
- Uncommitted: 3 untracked, pre-existing (not this session's work):
  `AGENT_MODEL_PIN_BUG_PROMPT.md`, `docs/handoffs/2026-06-04-1006.md`,
  `docs/handoffs/2026-06-04-1035.md`. Decide whether to commit or delete.

## What shipped this session
- dbc65b0 — fix(agents): PascalCase tool names so custom subagents get a real
  toolset (#22, squash-merged, CI green, branch deleted).

## Exact next steps
1. Fully quit + relaunch Desktop (NOT resume — resume keeps stale cached defs).
2. Run the re-test above on a fresh 1M thread.
3. If all PASS: update runbook status line to "F2 closed, 14/14 verified";
   commit. Otherwise re-open with transcript.
4. Tidy the 3 untracked files (commit the handoff archives, delete the bug prompt
   if obsolete).

## Gotchas
- Agent defs cached at boot. Never verify an agent edit in the session that made
  it; resuming a thread also keeps stale cache — start fresh.
- Cloud sessions can't test this (no local `~/.claude/agents/`, may not be 1M).
- The fix is live in `~/.claude/agents/` independent of git; `main` is provenance.
- Stack is 1 behind source — `run update.sh` when convenient.

## Team this session
- Used: `designer` ×1 (F2 smoke test — PASS, confirmed the fix).
