# Next-session handoff

_Written: 2026-06-20 18:28 PT_

## Resume prompt
> "Loop-eng stack is fully merged and `main` is green. Pick next on the loop-eng roadmap: (a) **ADR-021** (superpowers design-before-code gate — PreToolUse `Edit|Write` deny-until-spec), (b) define the **ultracode signal** (still zero hits in the stack; needed by ADR-019 ceiling-lift + ADR-021), or (c) **Phase 2** (Supabase `loop_runs` + live mid-flight cost monitor — closes the within-iteration runaway gap). Optional housekeeping: open PRs for the two `loop_policy` config branches pushed this session (see Cross-repo)."

## Branch & state
- Branch: `main` (worktree: N/A) — clean, up to date with `origin/main`.
- Uncommitted: clean (handoff files committed by this skill).
- Local branches: only `main` (all session branches pruned).

## What shipped this session
- `4df7dba` — feat(loop-eng): make `loop_policy` core scaffolding (Tier 2+) (**#39**) — **includes the merger fix** (`f49a11a` squashed in): `config-merger.sh` now collapses hook blocks sharing a `.matcher` + regen'd `capability-registry.json`. This **healed a red `main`**.
- `f9b30aa` — docs(handoff): prior-session archive (**#40**, rebased onto the fixed main).
- All 8 stack/config PRs from the prior handoff **merged** (#39, #40, #82, #3, #18, #52, #9, #83). Zero open PRs.

## What's blocked & why
- Nothing blocked.

## Exact next steps
1. Resume loop-eng roadmap — pick **ADR-021** / **ultracode signal** / **Phase 2** (see Resume prompt).
2. (Optional) Open PRs for the two `loop_policy` config branches pushed this session so they reach those repos' mains (see Cross-repo) — or fold them in those repos' own sessions.
3. Run `update.sh --tier=4` on any other machine — local stack here is already current.

## Gotchas
- **Root cause of the red `main`:** the merger's array rule `($a+$b)|unique` only deduped byte-identical objects. When loop-eng (#37/#38, already on main) added `irreversible-deny.sh` to the Tier-2 team template's `Bash` block, base+fragment merged into **two** `Bash` blocks → `bulk-job-reminder.sh` ran twice. The C-merge test caught it but the merger upgrade had never shipped. **Now fixed:** `merge_hook_groups` regroups by `.matcher`. **Authoring note — tier fragments may now freely re-list shared matchers; the merger combines them.**
- **Local `~/.claude/settings.json` was healed in place** this session (was running `bulk-job-reminder.sh` twice). Backup at `/tmp/settings.bak.json`.
- **Handoff "fork→upstream" warning was wrong:** billbot/daily-revenue-report/spend-enrichment PRs were base `CarboNet-Nano/*` (isFork=false) — no fork-sync needed.
- **Sandbox merged fine this session** (user authorized `gh pr merge`). Direct merges worked; no `gh pr create` was needed.
- **Team log under-captured again** — `session-start.txt` ts (01:28Z) postdated the work window, so per-session jq returns empty. Team notes below are from actual runs.

## Cross-repo references
Two repos had `loop_policy` backfilled this session, committed + pushed as **new remote branches with no PR yet**:
- `daily-finance-sync` — `09e6778` on branch `feat/po-denial-v4-envelope` (remote `bschonbrun/daily-finance-sync`).
- `manufacturing-dashboard` — `685774fd` on branch `chore/handoff-2026-06-20-1011` (remote `bschonbrun/carbonet-dashboards`).
- Both are merge-only additions (preserved existing config, bumped `last_modified`).

## Team this session
- Used: **1 Workflow** (`cleanup-state-audit`, 10 read-only agents) — parallel audit of 7 PRs + 2 staged repos for live merge/CI state. Unrostered (read-only, no roster roles), 0 write-heavy.
- Unrostered write-heavy workflows: 0
- Benched (should-have-fired):
  - **reviewer** — the `config-merger.sh` change is a merge-primitive edit; the stack norm is a cross-model (Codex) cold-read before merging such code. Skipped here (small, test-backed, CI-verified green) but worth a Codex pass if the merger is touched again.
