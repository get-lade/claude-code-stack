# Next-session handoff

_Written: 2026-06-03 14:13 PT_

## Branch & state
- Branch: `main` (worktree: N/A) — up to date with origin
- Uncommitted: clean
- PRs #18, #19 merged this session; no open PRs.

## What shipped this session
- 30b6cab — docs(phase0): record cloud gate FAILED — plugins unavailable in cloud (#19)
- ad5d5f9 — Plugin migration Phase 0 + Phase 1: author claude-code-stack plugin (#18)

## What's blocked & why
- **Plugin migration is PAUSED (decision C).** Phase 0 cloud gate FAILED — see below.
  Do NOT start Phase 2/3.

## The key finding (read first)
**Claude Code cloud sessions do NOT support plugins** (verified live 2026-06-03 via
Desktop Mac app → cloud session on `bschonbrun/stack-plugin-test` with the
`enabledPlugins` settings block committed). Cloud supports connectors + commands
but not plugins or adding folders — no trust prompt, Plugins panel greyed out,
`/goodmorning` unknown. This kills the migration's cloud premise (same dead-end as
org Skills). Recorded in `docs/phase0-verification.md` + project memory
`cloud-no-plugins.md`.

## Current state of the plugin
- The `claude-code-stack` plugin built in #18 (`.claude-plugin/plugin.json`,
  `marketplace.json`, `hooks/hooks.json`, dual-mode hook scripts) **validates and
  works for local/desktop CLI only.** It is inert for cloud.
- It does NOT affect running installs (absent from all tier-manifests → install.sh
  won't copy the plugin files into `~/.claude`).

## Exact next steps
1. **Nothing urgent.** Migration parked pending cloud plugin support.
2. If/when Claude Code cloud adds plugins: re-test with `bschonbrun/stack-plugin-test`
   (settings block already committed). If it auto-enables, resume Phase 2
   (`docs/plugin-migration-plan.md`).
3. Cloud distribution stays on the existing file-copy bootstrap
   (`scripts/cloud-bootstrap.sh`) + org reconciler — both already work in cloud.
4. Optional cleanup: decide whether to keep the local-only plugin or revert #18.
   (Recommendation: keep — it's harmless and ready if cloud support lands.)

## Gotchas
- Sandbox can't reliably push to `main`; branch + PR, then merge.
- `statusLine` is NOT a valid `plugin.json` key (must stay in settings.json) — matters
  only if the plugin route resumes (Phase 3 reconciler payload).
- Test repo `bschonbrun/stack-plugin-test` is private and kept for the re-test.

## Cross-repo references
- `bschonbrun/stack-plugin-test` — throwaway repo with the plugin enable-block; for
  the future cloud re-test only.
- `bschonbrun/claude-stack-admin` + `carbonet-nano/*` — unchanged; still on file-copy
  bootstrap (correct, given cloud has no plugins).

## Team this session
- Used: none (direct verification + edit + merge; no subagents dispatched).
