# Next-session handoff

_Written: 2026-06-20 17:55 PT_

## Resume prompt
> "Loop governor is now core scaffolding (PR #39 + 6 backfill PRs open). Pick next: (a) **merge the 7 PRs** + commit the 2 staged repos, (b) **ADR-021** (superpowers design-before-code gate), (c) define the **ultracode signal**, or (d) **Phase 2** (Supabase `loop_runs` + live cost monitor)."

## Branch & state
- Branch: `feat/loop-policy-core-scaffolding` (worktree: N/A) — PR [#39](https://github.com/bschonbrun/claude-code-stack/pull/39) open.
- Uncommitted (this repo): clean.
- Behind/ahead of origin: branch pushed, up to date.

## What shipped this session
1. **Dogfooded `/loop-engineer`** live (handoff next-step #1) — impossible criterion (`false`) + `max_iterations:3` → cap tripped, stop allowed, never ran forever. Per-session state (ADR-020) held: +1/stop, no double-count. **PASSED.**
2. **`90a4f42`** — feat(loop-eng): make `loop_policy` core scaffolding (Tier 2+). project-init emits the block (separate Tier≥2 snippet), SessionStart banner surfaces `/loop-engineer`, this repo's config backfilled. Codex-reviewed (4 blocking + 3 non-blocking, all fixed). Suite 93/0. → PR #39.
3. **Backfilled `loop_policy`** into all Tier≥2 repos (10 configs) via `script:backfill-loop-policy.sh` (merge-only, +change_history, ephemeral worktrees pruned).

## What's blocked & why
- Nothing blocked. 7 PRs awaiting merge (no auto-merge configured on these repos → manual merge).

## Exact next steps
1. **Merge the 7 PRs** (stack #39 first, then the 6 config backfills — see Cross-repo).
2. **Commit the 2 staged repos** — `daily-finance-sync` (branch `feat/po-denial-v4-envelope`) and `manufacturing-dashboard` (branch `chore/handoff-2026-06-20-1011`) each have `.claude/stack-config.json` staged; fold into their in-flight handoff commit.
3. Then resume loop-eng roadmap: **ADR-021** (superpowers gate), **ultracode signal** (still zero hits in stack), **Phase 2** (Supabase `loop_runs`, live mid-flight cost monitor — closes the within-iteration runaway gap).

## Gotchas
- **Forks PR into upstream:** `billbot`, `daily-revenue-report`, `spend-enrichment` are forks → their PRs target the **CarboNet-Nano** org, not `bschonbrun`. After merge, sync the forks.
- **carbonet-dashboards double:** `manufacturing-dashboard` + `md-adr008-pr2` share remote `bschonbrun/carbonet-dashboards`. loop_policy rides ONLY manufacturing-dashboard's branch; the dup in md-adr008-pr2 was discarded.
- **Backfill script lives in `/tmp/backfill-loop-policy.sh`** (not committed). Recreatable; recursive `find`, prunes `worktrees`/`node_modules`/`mfg-worktrees`, idempotent (skips repos that already have loop_policy).
- **Loop dogfood behaviors:** a user message **preempts** the Stop-hook (counter only advances when a turn ends with no input); the cap-trip emits **no** block message (silent allow, `status→max_iterations`).
- **subagent-runs.jsonl under-captured** again (session-start.txt ts = 00:49Z mismatched the work window) — team numbers below are from actual runs.

## Cross-repo references
7 PRs opened this session (all add `loop_policy` to `stack-config.json`; stack repo also has the scaffolding):
- [bschonbrun/claude-code-stack#39](https://github.com/bschonbrun/claude-code-stack/pull/39) (3 files)
- [bschonbrun/Architect-for-Claude-Code#82](https://github.com/bschonbrun/Architect-for-Claude-Code/pull/82)
- [bschonbrun/pd-gateway-mcp#3](https://github.com/bschonbrun/pd-gateway-mcp/pull/3)
- [CarboNet-Nano/carbonet-seepex-tracker#18](https://github.com/CarboNet-Nano/carbonet-seepex-tracker/pull/18)
- [CarboNet-Nano/billbot#52](https://github.com/CarboNet-Nano/billbot/pull/52)
- [CarboNet-Nano/daily-revenue-report#9](https://github.com/CarboNet-Nano/daily-revenue-report/pull/9)
- [CarboNet-Nano/spend-enrichment#83](https://github.com/CarboNet-Nano/spend-enrichment/pull/83)
- Staged (no PR): `daily-finance-sync`, `manufacturing-dashboard`.

## Team this session
- Used: **reviewer ×1** (Codex) — adversarial cold-read of the scaffolding diff; caught 4 blocking (footer tier-leak, backfill glob one-level-deep, arg-order bug, `LP="null"` corruption) + 3 non-blocking (failure exit code, template/prose conflict, wrong `invoked_via`). All resolved + verified.
- Benched: **architect** — a "core scaffolding" change could have had an architect-first pass; acceptable here (small, additive, config-only, Codex-verified). No financial/schema/deploy domain triggers fired, so validator/data-engineer/ops correctly idle.
