# Next-session handoff

_Written: 2026-06-25 03:33 PT_

## Branch & state
- Branch: `claude/mcp-reflect` (worktree: N/A)
- Uncommitted: clean (handoff files only)
- Behind/ahead of origin: 2 ahead (handoff commits not yet on main)

## What shipped this session
- `ce309cd` — feat(tier-1): Tavily+Context7 MCP servers and /reflect skill (#55)
- `339f591` — feat(goodmorning): model-audit freshness nudge (step 6f) (#54)
- `03c2423` — feat(rtk): integrate RTK token-reduction proxy at Tier 1 (#53)

Three Tier 1 features merged to main this session:

1. **RTK token-reduction proxy** — `hooks/rtk-rewrite.sh` PreToolUse hook wraps shell commands through `rtk rewrite`; `cloud-bootstrap.sh` installs RTK in cloud containers. Silent no-op if binary not installed. User still needs `brew install rtk` locally.

2. **Goodmorning model-audit nudge (step 6f)** — reads `last_audited` from `~/.claude/config/model-routing.json`; surfaces `Models: audit due` in boot summary if ≥30 days overdue. Currently 40 days overdue.

3. **Tavily + Context7 MCP + `/reflect` skill** — Tavily (real-time web search, OAuth) and Context7 (live library docs, free tier) wired into `config/settings.tier-1.template.json`. `/reflect` skill writes post-task retrospective to screen + `docs/retros/`.

## What's blocked & why
- Nothing blocked. All three PRs merged, main green.

## Exact next steps
1. Pull main locally: `git checkout main && git pull`
2. Run `scripts/install.sh --tier=4 --mode=merge --skip-requirements` to propagate all features into `~/.claude`
3. Install RTK binary locally if not done: `brew install rtk`
4. **Run `/model-audit`** — `last_audited` is 2026-05-16, 40 days overdue
5. Tavily first use: try a web search in Claude Code — OAuth browser window will appear
6. Context7: works immediately, no setup needed

## Gotchas
- `capability-registry.json` must be regenerated when adding new skills/agents — run `scripts/gen-capability-registry.sh` and commit or CI lint fails.
- RTK hook silently no-ops if binary not on PATH — hook and binary are independent installs.
- `prompt-caching.json` is documentation-only — no executable wiring needed; Anthropic caches automatically.
- Review router fail-safe: on ANY error, router MUST default `high` stakes, never routine. Don't touch `rr_classify_stakes` without tests.
- Handoff commits must land on main; if on a feature branch use `chore/handoff-*` branch + PR.

## Team this session
- No subagent log found — all work done in main thread.
