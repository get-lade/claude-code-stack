# Next-session handoff

_Written: 2026-06-19 14:11 PT_

## Resume prompt
> "Resume the control-panel plan, egress is open."

## Branch & state
- Branch: `claude/goodmorning-6s00t9` (worktree: N/A)
- Uncommitted: clean (only this handoff lands here)
- Behind/ahead of origin: even with origin/main at session start; this handoff is the only commit on the branch

## Goal of next session — PLAN 3 bundled features (Tier-4 full-team + cross-model adversarial)
No code yet. Deliverable is an **approved plan** (ADR + interface contracts + control-panel UX), then `/review-handoff`. The three features:

- **A. `/suggest` skill** — real-time, **thread-scoped** recommender. Reads current conversation + last user ask, replies with relevant stack skills/agents/commands + one-line each ("ask me more about any"). Narrower than `/insights`: this thread only, no analytics log. Trigger example: "can we use more than one agent?" → surfaces `/foreman`, `/agent-teams`, dynamic-workflows.
- **B. Always-watching hook** — `UserPromptSubmit` hook, sibling to `hooks/dispatch-nudge.sh`, passively detects when a stack capability is relevant and injects a nudge system-reminder. Gated by a **new on/off stack setting surfaced in `/session`**. Open question for architect: should A + B share ONE recommendation engine? (product-critic was asked to weigh this.)
- **C. "Control panel" config skill** — like `/config` but walks the user through **EVERY** stack setting (guided prompt walkthrough and/or "visual modal"). Must scale to many settings. Architect to decide: monolith vs **router over existing setting-skills** (`/tier`, `/domain-mode`, `/strict-mode`, `/sensitivity`, `/cost-cap`, `/session`, `/default-edit`). Note cloud reality: no real GUI, only `AskUserQuestion` as a "modal" primitive.

## What's blocked & why
- **Was blocked, now cleared by user:** cross-model critics (Codex/Gemini) were blocked by the cloud env's egress policy (`api.openai.com` + `generativelanguage.googleapis.com` denied). **User reports egress is now open for both hosts.** Verify at session start before dispatching critics.

## Exact next steps
1. **Verify egress** is actually open (both keys are SET; both CLIs missing → direct-API path):
   - `OPENAI_API_KEY` set, `GEMINI_API_KEY` set, `codex`/`gemini` NOT on PATH.
   - Confirm a critic can reach `api.openai.com` / `generativelanguage.googleapis.com`. If still blocked, fall back to Architect + Claude-only adversarial (the user's last-resort preference is open — re-ask if hit).
2. **Re-run the full team via `/foreman`** in this sequence (main-thread orchestration, Tier 2):
   - **product-critic** (Codex) — challenge WHAT; the full brief is below in "Product-critic brief". It returned BLOCKED last session (egress), so it has not yet run.
   - **architect** — ADR + interface contracts + control-panel workflow/UX, consuming product-critic output.
   - **reviewer** (Codex) ∥ **red-team** (Gemini) — parallel adversarial cold-read of the plan (both read-only, safe to parallelize).
   - **scribe** + **`/review-handoff`** gate before declaring the plan done.
3. Compose foreman final report; present plan to user for approval **before** any implementation.

## Product-critic brief (re-use verbatim — it never ran)
Challenge WHAT (not how) for the 3 features above. Specifically answer:
1. Is each the right problem? Highest/lowest value? Cut or defer any?
2. **Overlap risk:** `/suggest` (A) vs always-watching hook (B) vs existing `dispatch-nudge` — 3 things doing 1 job? Should A+B share one engine?
3. **Control panel (C):** is "every setting" a YAGNI trap? Router-over-existing-skills vs new monolith? Does "visual modal" survive cloud-no-GUI?
4. Noise/annoyance: failure mode where users disable hook B day one? What metric proves A/B earn their keep?
5. If only ONE ships this week, which, and why?
Return per-feature verdict (build/reshape/cut/defer), top-3 risks, recommended scoping for architect.

## Gotchas
- **Cloud has no slash-commands** — invoke skills by name ("run foreman", "run suggest"). `/`-commands only work on desktop CLI.
- **Cross-model is the point** of the adversarial pass (ADR-011 Codex, ADR-012 Gemini). Do NOT substitute Claude-only critiques silently — if egress still blocked, surface it and ask (user already chose "open egress, then full team").
- A prior product-critic dispatch tripped a **SECURITY WARNING** only because it *attempted* the (blocked) outbound call — nothing left the sandbox. Expect the warning to stop appearing once egress legitimately allows those hosts.
- Sandbox can push feature branches + open PRs but **cannot push directly to `main`**.
- This is the stack **source** repo (dogfoods Tier 2). Hooks/skills/agents are global (`~/.claude`), reached via `scripts/update.sh` per machine — but this planning work changes only repo files.

## Team this session
- Used: product-critic ×1 (returned BLOCKED — egress; no critique produced).
- Benched (deferred by egress block, not a routing miss): architect, reviewer, red-team, scribe — all queued for next session per Exact next steps.
