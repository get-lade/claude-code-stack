# Next-session handoff

_Written: 2026-06-04 11:12 PT_

## ⚡ START HERE — F2 is CLOSED. Nothing urgent open.

F2 (custom subagents emit tool calls as literal text → `tool_uses: 0`) is fully
resolved and shipped. Re-tested **22/23 dispatchable agents** on a fresh 1M boot
(`claude-opus-4-8[1m]`) — all returned `tool_uses: 1` with accurate facts and
correct model pins. Merged in #23. Runbook status → **CLOSED**.

The session that opens on this handoff has **no required work**. Optional items
below.

## Branch & state
- Branch: `main` (worktree: N/A)
- Uncommitted: clean
- Behind/ahead of origin: up to date (last: eb3c575, #23)
- Local stack: updated via `scripts/update.sh --tier=2` (69/69 checks pass, current)

## What shipped this session
- eb3c575 — docs: close F2 — 22/23 agents verified on fresh 1M boot (#23)
  (runbook → CLOSED, archived 3 handoffs, removed scratch bug-prompt)

## What's blocked & why
- Nothing blocked.

## Exact next steps (all OPTIONAL)
1. `local-ops` is the only unverified agent — model `ollama/llama3.1:8b`. To close
   the last 1/23: ensure a local ollama runtime is up, then dispatch it with the
   same Read smoke test (`agents/local-ops.md` → line 1, line count, `model:`),
   ground-truth via Bash. PASS = `tool_uses: 1` + accurate facts.
2. Cloud is unverified by direct dispatch (no 1M cloud session per prior handoff),
   but the fix rides the same install path (`cloud-bootstrap.sh` → `install.sh`
   copies fixed agents). If you want belt-and-suspenders: open a cloud session on
   this repo and dispatch one agent with the Read smoke test.
3. Separate, pre-existing goal still parked: plugin migration is blocked because
   cloud has no plugins (see MEMORY: cloud-no-plugins). Unrelated to F2.

## Gotchas
- Agent defs cache at session boot. Never verify an agent edit in the session that
  made it; resuming a thread keeps stale cache — start a FRESH session.
- Agents are global (`~/.claude/agents/`), not per-repo. The fix applies to every
  stack repo on the machine automatically; the source repo `main` is just provenance.
- Other machines pick up the fix via `git pull && scripts/update.sh`. Cloud picks
  it up automatically at session start. Both need a Claude Code restart after update.
- Sandbox CAN push branches + open/merge PRs here (did so for #23). Cannot push
  directly to `main` — always branch.

## Team this session
- Used: 22 smoke-test dispatches — designer ×1 (goodmorning prior), then reviewer,
  implementer, validator, accessibility-auditor, architect, architecture-critic,
  data-engineer, documenter, estimator, historian, incident-commander,
  integration-specialist, librarian, ops, performance-engineer, product-critic,
  red-team, scribe, security-auditor, tester, foreman-team-lead ×1 each (F2 re-test).
- Benched: none of note — this session's purpose WAS exercising the roster.
