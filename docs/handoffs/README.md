# Session handoffs — internal only

Handoffs written by `/handoff` land here. They routinely name real repos, PRs, and
tenant details, so this directory is **lade-internal**: it must never ship in any
tenant-facing distribution artifact (pack installer, reconciler payload, release
tarball — all exclude `docs/handoffs/`).

Handoffs from before 2026-07-21 were archived to the CarboNet tenant pack repo under
`docs/stack-history/handoffs/` during the M1 privatization scrub.
