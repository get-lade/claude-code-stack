# Next-session handoff

_Written: 2026-06-21 14:05 PT_

## Branch & state
- Branch: `main` (worktree: N/A)
- Uncommitted: clean
- Behind/ahead of origin: up to date (`b626405`)

## What shipped this session
- `b626405` — fix(loop-eng): kill cost double-count + scope design-gate global fallback ([#50](https://github.com/bschonbrun/claude-code-stack/pull/50), squash-merged)

Closed both non-blocking follow-ups carried in from the 2026-06-21 consolidated handoff:
1. **`hooks/loop-stop.sh`** — cost accrual no longer double-counts. `_ITER_COST` read
   from `subagent-runs.jsonl` is a since-start CUMULATIVE total; it was being ADDED
   onto the already-cumulative `cost_so_far_usd` each Stop invocation (masked until
   now only by the ADR-024 `max` guard when a live per-tool writer existed). Now:
   cumulative log reads REPLACE, only the no-log per-iteration estimate ACCUMULATES;
   final = `max(new, live, prev)` for monotonicity. Proven 0.30→0.30 across two Stop
   runs (was 0.30→0.60).
2. **`hooks/design-gate.sh`** — global approval can no longer leak across sessions.
   When a `session_id` resolves, the per-session marker is authoritative and the
   shared unscoped `design-approved.json` is NOT consulted. Legacy unscoped file is
   honored only when no SID resolves (back-compat preserved).

Test suite: **PASS=183 FAIL=0** (added a gate leak-closed test; updated the
legacy-fallback assertion to the new no-SID-only semantics).

## What's blocked & why
- Nothing blocked. Both follow-ups closed and merged; main is green.

## Exact next steps
1. Start from `main` (`git checkout main && git pull`) → `b626405`.
2. Loop-engineering roadmap (Phases 1–3 + ADR-024 polish + both cleanup follow-ups)
   is now fully complete. Pick fresh work.

## Gotchas
- Sandbox CAN push + merge this session (user granted it explicitly); don't assume
  that's standing for future sessions — confirm before pushing/merging.
- `hooks/loop-stop.sh` cost path now has two distinct semantics: real-log reads are
  cumulative-since-start (replace); no-log estimate is a per-iteration delta
  (accumulate). The `_ITER_CUMULATIVE` flag gates which. Keep that invariant if you
  touch the accrual block — mixing them is the bug just fixed.
- `design-gate.sh` legacy unscoped marker is now reachable ONLY with no resolvable
  SID. Tiny back-compat edge: if `/plan` wrote the global file because its env SID
  was empty but the gate later sees a payload SID, that same-session approval won't
  be honored. Accepted tradeoff (closes the cross-session leak).

## Team this session
- Used: none (single-thread direct edit; 2-file cleanup + test).
- Benched (soft): `reviewer` — a security-relevant change to the design-gate
  approval path merged without an adversarial Codex cold-read. Low risk (covered by
  +1 leak test, full suite green), but a cold-read would have been cheap insurance.
