# Next-session handoff

_Written: 2026-07-21 15:05 PT_

## Branch & state
- Branch: `main` (worktree: N/A) — synced, PR #79 merged
- Uncommitted: handoff files only
- Origin: up to date

## What shipped this session
- b9413b5 (#79) — M1: scrub tenant-specific content from core + ADR-034 tenant pack contract
- CarboNet pack PR #3 (companion) merged: `docs/stack-history/` archive + `agents/designer.md` CarboNet override

Context: agreed to merge `claude-code-stack` (now private lade IP, `get-lade` org) + `carbonet-vibe-coding-standards` into **lade dev-stack module (private core) + repo-per-tenant packs** (CarboNet = pack #1), with `/project-init` auto-provisioning GitHub + Neon(default)/Supabase + Clerk + **Cloudflare** hosting. Full plan: `~/.claude/plans/i-d-like-you-to-prancy-firefly.md`.

## What's blocked & why
- **M2 privacy flip is BLOCKED on a human secret step.** `get-lade/claude-code-stack` is still **PUBLIC**. Before flipping to private: user must create a fine-grained read-only PAT (Contents:Read on stack + all pack repos) and set `CLAUDE_STACK_REPO_TOKEN` in (a) each Claude cloud environment and (b) the claude-stack-admin reconciler secrets. Claude cannot handle secrets. Flip visibility only AFTER token is live, or cloud sessions of the 6 enrolled repos break.

## Exact next steps
1. **M0 spikes** (can start now, no blockers): Clerk app-creation API feasibility; Neon `POST /projects` create smoke; Pipedream project-create via pd-gateway-mcp. Write results to `docs/plans/`.
2. **M2** (after user sets token): activate `CLAUDE_STACK_REPO_TOKEN` path in `scripts/cloud-bootstrap.sh`, default repo → `get-lade/claude-code-stack`, fix `bschonbrun/...` URLs in `.claude-plugin/plugin.json` + `templates/stack-config.template.json` $schema + README. THEN user flips repo private.
3. **M3** pack mechanism: `schemas/tenant-pack-schema.json`, `scripts/lib/pack-installer.sh` (overlay-wins — inverts config-merger target-wins, per ADR-034/ADR-013 amendment #1), `install.sh --pack`, `stack-defaults` `tenant_pack` ref.

## Gotchas
- Local stack remote WAS stale (`bschonbrun/...`) — fixed this session to `get-lade/...`. Verify on fresh clones.
- pack-installer merge semantics are the sharp edge: core<pack but pack<project — two different conflict-winners. Opus + a test (overlay-wins) required. Don't reuse `merge_json` defaults.
- Build routing: Sonnet for markdown/scripts/scrubs; Opus for pack-installer merge, ledger idempotency/rollback, ADRs, M2 sequencing + M5 provisioning review.

## Cross-repo references
- Pack #1: `CarboNet-Nano/carbonet-vibe-coding-standards` (PR #3 merged). Standards still in dual trees (`STANDARDS/` + `.antigravity/rules/`) — M4 collapses to one `standards/` tree, Neon-default DB rewrite, drop `management.md` (commit-to-main drift) + `supabase.md` DROP CASCADE, generate Gemini from single source.
- Engine (later, separate repo): `Architect-for-Claude-Code/docs/plans/2026-07-19-provisioning-engine-standalone.md`.
- Provisioning skill v1 to port: `~/.claude/skills/app-bootstrap/` (orphaned, installed-only).

## Which repo do other Claude accounts work from?
- **Core work (agents/skills/hooks/installer/provisioning):** `get-lade/claude-code-stack` — branch-per-task, PR to `main`.
- **CarboNet pack (standards/palette/tenant.json):** `CarboNet-Nano/carbonet-vibe-coding-standards`.
- Cross-repo M-milestones (M1, M4) touch both — pair the PRs and reference each other, as #79 ↔ #3 did.
