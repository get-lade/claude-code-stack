# Next-session handoff

_Written: 2026-06-20 07:58 PT_

## Resume prompt
> "Loop-engineering control plane Phase-1 is merged (#37) + ADR-019 accepted. Pick next: (a) **dogfood** `/loop-engineer` in a FRESH session, (b) write **ADR-020** (superpowers design-before-code gate), (c) define the **ultracode signal**, or (d) start **Phase 2** (Supabase `loop_runs` + live cost monitor)."

## Branch & state
- Branch: `main` (worktree: N/A) — up to date with origin, clean.
- PR #37 merged as `d491aaa` (squash). Feature branch `docs/loop-engineering-spec` can be deleted.

## What shipped this session
- `d491aaa` — feat(loop-eng): loop-engineering control plane — spec, plan + Phase-1 build (#37)
- Includes **ADR-019** (Accepted), the design spec, the TDD plan, and the full Phase-1 build.
- Phase-1 artifacts: `skills/loop-engineer/` (SKILL.md + loop_lib.sh), `hooks/loop-stop.sh` (fail-closed Stop hook), `hooks/irreversible-deny.sh`, `loop_policy` in `schemas/stack-config-schema.json` + template, foreman loop-shape routing, tier-2 wiring, `tests/test-loop-engineering.sh` (**87/0**).

## What's blocked & why
- Nothing blocked. Phase-1 is self-contained; the open items below are new work, not blockers.

## Exact next steps
1. **Dogfood `/loop-engineer` in a FRESH session** (NOT a writing session — the loop Stop-hook would block that session's own stops). First `cd claude-code-stack && ./scripts/update.sh --tier=4` so the new hooks/skill are installed into `~/.claude` (the merge only updated the repo). Then run a real loop with a machine-checkable criterion, e.g. goal "loop until `bash tests/test-loop-engineering.sh` passes" against a deliberately-broken change. Verify: Stop-hook blocks until criterion passes; `max_iterations`/timeout/no-progress bounds trip; `irreversible-deny` fires on `git push` during an active loop; `pattern selected: X` logs.
2. **Write ADR-020** — superpowers design-before-code gate. Vendor `using-superpowers` (Tier 0/1) + brainstorming's text core (drop the visual-companion Node server); a PreToolUse `Edit|Write` `permissionDecision: deny`-until-spec-exists hook (reuses ADR-019's governance-hooks base); reconcile with the existing `/plan` skill (replace / wrap / mode).
3. **Define the ultracode signal** — grep returns zero hits in the stack today. A config flag / env the stack can read; needed by ADR-019's autonomy ceiling-lift AND ADR-020's gate. Decide where it lives (stack-config? settings.json? session-state?).
4. **Phase 2** — Supabase `loop_runs` table (Tier 3+), live mid-flight cost monitor (closes the within-iteration runaway gap), per-iteration telemetry to `subagent-runs.jsonl`, optional `model_effort` enum widening (touches ADR-018 dashboard).

## Gotchas
- **Dogfood needs a fresh session** — the loop Stop-hook interferes with the authoring session that sets the loop up.
- **`irreversible-deny` is best-effort defense-in-depth, NOT a security boundary** (documented Phase-1 limit). Pattern-matching can't catch `sudo -u` / `env -i` / encoded cmds. The real boundary is autonomy ceiling + human checkpoint + Claude Code permissions. Do not chase bypass-proofing.
- **Cost cap is advisory in Phase 1** (between-iteration, data-dependent). The live halt is Phase 2. `max_recursion_depth` is advisory (`max_iterations` is the hard cap).
- **Hooks not yet active in `~/.claude`** — they shipped in the repo via the merge; run `update.sh` to install them locally before dogfooding.
- **Gemini CLI still tier-blocked** — route adversarial review via **Codex** (worked very well this session: caught awk injection, HOME-unset crash, `"auto"`-termination bypass, cost double-count).
- Sandbox COULD push branches + open/merge PRs this session — confirm that still holds next session.

## Cross-repo references
- None. The stack source repo dogfoods itself (Tier 4).

## Team this session
- Used (via Workflow tool): **Explore** ×~13 (3 context maps + research + plan-grounding fan-outs), **implementer** (Sonnet) for the 8-task TDD build + 3 fix rounds, **reviewer** (Codex) for per-task + 2 cross-cutting adversarial passes. Direct dispatch: reviewer ×1 (final whole-impl review).
- `subagent-runs.jsonl` under-captured workflow roster this session (only 1 row logged) — team numbers above are from actual runs, not the log.
- Benched (note for next session): **data-engineer** — Phase-2 `loop_runs` is a schema-migration; data-engineer should fire then. architect/ops/validator acceptably idle (work was spec-driven author-direct + Codex-verified).
