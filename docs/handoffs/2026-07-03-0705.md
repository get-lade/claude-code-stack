# Next-session handoff

_Written: 2026-07-03 07:05 PT_

## Branch & state
- Branch: `main` @ `669a588` — clean, all feature work merged
- Uncommitted: none tracked (only session scratch: `.claude/context/`, `docs/stack-refreshes/` — safe to delete)
- Behind/ahead of origin: up to date

## What shipped this session
Three coordinated threads in this repo, all merged to `main`:
- `156182d` — **#66 ADR-030**: OpenAI review via the API by default; Codex CLI becomes an opt-in transport (`codex_transport=api|cli`) that always falls back to the API. New `scripts/lib/openai-review.sh` (single transport source of truth; `oair_call`; perl+bash-watchdog timeouts; ADR-011 Claude-model guard). Transport-aware preflight. Advisory codex tier check. `stack_version` 1.1.4→1.1.5.
- `8c289fb` — **#68 ADR-031**: tier-3 Gemini dependency is now a soft `gemini-api-key` advisory check, not the retired CLI (ADR-027). Fixes a fresh-Tier-3 install hard-fail.
- `669a588` — **#69 hardening**: keep API keys off `curl` argv (`-H @-`) + exact-host base-URL allowlist across all three cross-family helpers (openai/gemini/deepseek). New `tests/test-cross-family-hardening.sh`.
- `8c27fe7` — #67: intermediate handoff (superseded by this one).

Root problem fixed: on this box the `codex` CLI is malware-blocked and **hangs**; the old preflight probed PATH presence, reported READY, then dead-stopped mid-`codex exec` with no API fallback. Now the API is the default and the CLI can't strand a review.

## What's blocked & why
- Nothing blocked. `main` green, no open PRs.

## Exact next steps
1. **Cleanup (optional):** prune merged local branches + worktrees — `feat/adr-030-codex-api-transport`, `chore/vendor-helper-hardening`, `chore/handoff-*`, worktrees `clever-kapitsa-e25ada` (merged) and `exciting-meitner-450a99`. `/cleanup-worktrees` handles it. `rm -rf .claude/context/`.
2. **Optional ADR-030 follow-up:** its advisory requirement branch is `command`-only, not `keychain_item` — so an `advisory:true` Keychain requirement still hard-fails (ADR-031 sidestepped this by dropping the requirement and relying on `advisory_smoke_tests`). Add a `keychain_item` advisory branch in `tier-installer.sh` if you ever want hard-gated-but-optional Keychain reqs.
3. **Carryover (unrelated, from the 07-01 handoff):** `/model-audit` was due (last 2026-05-16). And the open design thread: whether the project-root CLAUDE.md template should embed the Karpathy ten rules or stay global-only.

## Gotchas
- **This box's `codex` CLI is malware-blocked and HANGS** (`codex --version` never returns) — that is the whole reason api-transport is the default. Do NOT "fix" by reinstalling codex; api is the correct state.
- **Squash-merge breaks stacked branches:** #68/#69 were stacked on the real `c42f641`, which squash-merging #66 replaced with a new SHA — so they showed CONFLICTING until rebased with `git rebase --onto origin/main c42f641 <branch>` (drops the duplicated commit, replays only the delta). Standard pattern for the next stacked PR set.
- **This environment's auto-mode classifier refuses AI-initiated merges** — each merge needed an explicit human "merge it". Plan for that when handing PRs to a sandbox session.
- Cost note: OpenAI **API** review spend is billed to the key and is NOT in `cost_log` (same as gemini/deepseek); flagged for `/model-audit`.

## Cross-repo references
- Single repo. The two sibling worktrees (`clever-kapitsa-e25ada` = ADR-031, `exciting-meitner-450a99`) are now merged/idle and can be reaped.

## Team this session
- Used: architect ×1 (ADR-030 design + contracts), architecture-critic ×1 (Gemini/API → REWORK, folded), reviewer ×1 (dogfood via the new API path — found the 180s-timeout bug). Plus two coordinated peer threads (gemini advisory → #68, vendor hardening → #69), both verified here before push/merge.
- Benched: none material — design→critique→implement→dogfood→coordinate chain fully exercised.
