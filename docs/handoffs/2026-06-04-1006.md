# Next-session handoff

_Written: 2026-06-04 10:06 PT_

## ⚡ START HERE — this session has ONE job: verify the 1M subagent fix

The fix shipped last session (`b9c9b37`, already on `origin/main`). It can only
be verified on a **fresh 1M-context boot** — which this session is. Run the
smoke test below and report PASS/FAIL.

### The fix (already landed, do not re-edit)
Every `agents/*.md` had a fully-qualified model pin (`anthropic/claude-opus-4-8`).
When the parent runs 1M mode (`claude-opus-4-8[1m]`), the harness appended the
`[1m]` suffix to the pin → invalid subagent model id → **every** custom subagent
failed to spawn. Fixed by switching all `model:`/`escalation_model:` to bare
aliases (`opus`/`sonnet`/`haiku`); `local-ops` ollama untouched. Installed copies
in `~/.claude/agents/` were synced too. Full writeup:
`docs/runbooks/stack-cloud-overrides.md`.

### Smoke test — do this first
1. Confirm parent is 1M: model id should be `claude-opus-4-8[1m]`.
2. Dispatch the **`designer`** subagent (Agent tool, `subagent_type: designer`,
   NO `model:` override — we are testing the frontmatter pin) with this task:
   > Read the file `agents/designer.md` and report: (a) the exact text of line 1,
   > (b) the total line count, (c) the value of the `model:` field. Return only
   > those three facts.
3. **PASS (F1 fixed)** requires ALL of:
   - spawn succeeds (no "issue with the selected model …[1m]" error),
   - the agent's result shows `tool_uses >= 1` (it actually called Read),
   - the returned facts are **accurate** — independently Read `agents/designer.md`
     yourself and check line 1 + line count + `model:` match. Hallucinated
     (wrong) values = FAIL even if it "answered".

### If PASS
- F1 confirmed. Update `docs/runbooks/stack-cloud-overrides.md` status line to
  `F1 fixed + verified <date>`. Done — nothing else required.

### If FAIL — two cases
- **Spawn error still mentions `[1m]`** → bare aliases also get the suffix. Apply
  the documented fallback: drop the pins entirely (delete `model:` /
  `escalation_model:` lines, or set `model: inherit`) in `agents/*.md`, re-sync
  to `~/.claude/agents/`, commit, and hand off for ANOTHER fresh-boot re-test
  (caching means you can't verify in the same session).
- **Spawn OK but tool calls come out as literal text** (`<invoke name="Read">…`,
  `tool_uses: 0`, hallucinated content) → that's **F2**, a suspected Claude Code
  harness bug (custom agents get no live tool channel; `general-purpose` does).
  NOT a stack bug. Capture the exact transcript and file upstream to Anthropic.
  Record date + ticket in the runbook (F2 section).

## Branch & state
- Branch: `main` — up to date with origin (0 ahead / 0 behind).
- Uncommitted: only `AGENT_MODEL_PIN_BUG_PROMPT.md` (untracked — the original bug
  prompt; not mine to commit, leave or delete as you like).

## What shipped last session
- `b9c9b37` — fix(agents): bare model aliases so subagents survive 1M-context
  parent (+ `docs/runbooks/stack-cloud-overrides.md`). Already on `origin/main`;
  no PR (was pushed to main directly).

## Gotchas
- **Agent defs are cached at session start.** Any further edit to `agents/*.md`
  needs a full quit + relaunch before it can be tested. Never verify an agent
  edit in the same session that made it.
- Sandbox can't push to `main` or `gh pr create` — hand off for those.
- The fix is live in `~/.claude/agents/` independent of git; git is provenance.

## Team last session
- Used: none (direct edit + verification; no subagents dispatched).
