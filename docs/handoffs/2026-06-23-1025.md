# Next-session handoff

_Written: 2026-06-23 10:25 PT_

## Branch & state
- Branch: `main` (worktree: N/A)
- Uncommitted: clean (handoff files committed in this step)
- Behind/ahead of origin: up to date (`a6ffb4f`)

## What shipped this session
Built **ADR-025 — the adversarial-review model router** (tier review cost by stakes), across two merged PRs:
- `379db79` — feat(review): ADR-025 router ([#51](https://github.com/bschonbrun/claude-code-stack/pull/51))
- `a6ffb4f` — fix(review): install-manifest registration + cloud ollama fallback ([#52](https://github.com/bschonbrun/claude-code-stack/pull/52))

What it does:
- `scripts/lib/review-router.sh` — sourced by reviewer / security-auditor / product-critic. Classifies a diff's **stakes** → tier:
  - **high** (auth/crypto/payment/migration/RLS paths · `STACK_DOMAIN_MODE` security|schema-migration · sensitivity=high) → Codex `gpt-5.5`@high
  - **routine** (else) → local `qwen2.5-coder:32b` first → escalate Codex `gpt-5.4` (prose judgment)
- **Scope fix:** all tiers review the diff (`base..head`), not whole-repo.
- **Fail-safe → high** on any error/invalid override/git failure (was failing *open* to routine — caught in review).
- **Cross-family (ADR-011) enforced at resolution** — `rr_resolve` refuses Claude-family ids from env/config.
- **Config-driven models** (`env > config/model-routing.json .review_tiers > default`) — Tier-5 repoints with no script edit.
- **Cloud-safe:** no ollama → `rr_run` auto-routes routine to `codex/gpt-5.4` (`RR_LOCAL_FALLBACK=yes`).
- **Route logging:** `event:"review_route"` rows in `subagent-runs.jsonl` to verify the ~27/73 high/routine split.
- Tests: `tests/test-review-router.sh` 34/34; full suite **217/0**.

Cost target: **$200 → ~$60/mo** (scope fix + tiering). Codex was the only real bill; Gemini adversarial spend confirmed ~CA$57/mo (small, deferred).

Propagation: re-ran `scripts/update.sh --tier=4` → router now live in `~/.claude` (all local stack repos pick it up).

## What's blocked & why
- Nothing blocked. Both PRs merged, main green, propagated locally.

## Exact next steps
1. Start from `main` (`git checkout main && git pull`) → `a6ffb4f`.
2. **Verify the savings empirically** (~2 weeks out): run
   `jq -rs 'map(select(.event=="review_route"))|group_by(.stakes)|map({(.[0].stakes):length})' ~/.claude/logs/subagent-runs.jsonl`
   — confirm the high/routine split is ~27/73. If `high`% is much higher, the expanded regex over-fires → tighten `RR_HIGH_STAKES_RE`.
3. **Other machines:** when set up, run the stack update + ensure `ollama` + `qwen2.5-coder:32b` are pulled (routine tier needs them).
4. **Cloud:** code is ready (manifest + fallback). Real confirmation needs a cloud session with `OPENAI_API_KEY` in env — run a routine + a high-stakes review and confirm the router picks codex correctly.
5. **Tier-5 local-model expansion** (own plan/ADR) when the new high-RAM/GPU machine arrives — repoint `review_tiers` routine/escalation (and maybe high) to a bigger local model (Qwen2.5-Coder-72B / Kimi-K2 / DeepSeek-V3-class). A strong enough local model can absorb the high tier → OpenAI review spend toward $0.

## Gotchas
- **Router fail-safe direction is load-bearing:** on ANY ambiguity/error the router MUST default `high`, never routine. Both adversarial reviewers caught it failing *open* to routine. Keep that invariant if you touch `rr_classify_stakes`.
- **Manifest registration is required for propagation:** new `scripts/lib/*.sh` won't ship to `~/.claude` unless added to `config/tier-manifests/tier-N.json` `.files.global`. The first PR shipped the lib + agent references but not the manifest entry → unshippable to a clean install despite green tests. Always add the manifest entry + a `test -x` verify line.
- **Design-gate (ADR-021, ultracode):** source edits need an approved marker. `/plan` writes `~/.claude/session-state/design-approved.<sid>.json`; `approved_paths` globs match the **absolute** file_path, so prefix with `*/` (e.g. `*/scripts/lib/foo.sh`), not repo-relative.
- **Gemini reachable via API, not CLI:** the `gemini` CLI is tier-blocked (IneligibleTierError), but architecture-critic/red-team reach `gemini-2.5-pro` via direct `GEMINI_API_KEY` API call (ADR-012/015). Don't route those to Codex by default anymore — see updated memory.
- Sandbox COULD push + merge this session (user granted explicitly). Don't assume standing for future sessions — confirm before pushing/merging.

## Team this session
- Used: reviewer ×1 (Codex/gpt-5.5 cross-family), architecture-critic ×1 (Gemini-2.5-pro via API).
- Both fired on the right change (engine/governance edit) and surfaced 4 BLOCKING + design risks — all fixed before merge. No benched misses (not financial/schema/deploy domain; reviewer covered the injection/cross-family surface).
