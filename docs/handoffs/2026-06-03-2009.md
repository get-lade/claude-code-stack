# Next-session handoff

_Written: 2026-06-03 20:09 PT_

## Branch & state
- Branch: `main` (worktree: N/A) — up to date with origin
- Uncommitted: clean
- All this session's PRs (#10–#16) merged.

## What shipped this session
- 818ae63 — docs: scope plugin migration plan (#16)
- 5ef2610 — Reconciler auto-writes default stack-config.json into enrolled repos (#15)
- 07167a4 — docs(team-admin): operating runbook in admin repo README (#14)
- 9bef898 — Org-wide cloud distribution: /team-init + reconciler Action (#13)
- d15a63c — /cloud-setup skill + 3-way cloud prompt in /project-init (#12)

The org reconciler is **live** for `carbonet-nano`: 6 repos enrolled (topic
`claude-stack`), bootstrap PRs merged, repos self-install the stack + get a
default tier-2 `stack-config.json` in cloud sessions. Admin repo:
`bschonbrun/claude-stack-admin` (hourly cron + manual run, PAT secret set).

## Exact next steps — build the plugin migration
Read **`docs/plugin-migration-plan.md`** first. Then:

1. **Phase 0 — verify (BLOCKING, do before any build).** On a live `claude`:
   - Confirm a plugin loads in a **cloud** session (web/iOS).
   - Confirm repo `.claude/settings.json` `enabledPlugins` + `extraKnownMarketplaces`
     auto-enables after the one-time workspace-trust, with **no** per-user install.
   - Confirm whether **Team-plan managed settings** exist (decides approach B3).
   - Confirm `claude plugin validate` and the CLI field names actually exist in
     the installed build (several spec items are UNVERIFIED).
2. **Only if Phase 0 passes**, proceed Phase 1: author `.claude-plugin/plugin.json`
   (`name: claude-code-stack`) + `hooks/hooks.json`, rebasing hook/`lib` paths to
   `${CLAUDE_PLUGIN_ROOT}`. `skills/` and `agents/` are already at repo root (the
   default plugin locations) — low effort. **Hooks are the main cost.**
3. Phase 2: `.claude-plugin/marketplace.json` (this repo as its own marketplace).
4. Phase 3: slim the reconciler to stamp the enable-block into tagged repos
   instead of copying files.
5. New ADR recording the plugin decision (supersedes ADR-007); update `docs/CLOUD.md`.

Recommendation in the plan: **A1 + B1 + C1** — one plugin, this repo as the
marketplace, reconciler slimmed.

## Gotchas
- **Phase 0 is gating** — do not start authoring before cloud auto-enable is
  verified on a real build. The plan's spec came from a docs sweep; treat
  CLI flags / managed-settings / version-pinning as UNVERIFIED.
- Hooks today assume absolute `~/.claude/...` paths + `$CLAUDE_PROJECT_DIR`;
  in a plugin they must use `${CLAUDE_PLUGIN_ROOT}` and live in `hooks/hooks.json`.
- macOS CI runner is **bash 3.2** — no `mapfile`/bash-4 features in shell that
  runs in tests (bit us in `reconcile.sh`; fixed).
- This session's GitHub MCP scope is limited to `bschonbrun/claude-code-stack`;
  it could not create/write `claude-stack-admin` or read `carbonet-nano` —
  those steps were done by the user locally.
- Plugin work and the existing file-copy bootstrap must not double-wire hooks
  during migration (Phase 4 retires the bootstrap).

## Cross-repo references
- `bschonbrun/claude-stack-admin` — the live org reconciler (config: org=carbonet-nano,
  topic=claude-stack, enabled, secret `STACK_RECONCILE_TOKEN`). A slimmed
  reconciler (Phase 3) lands here and must be re-synced into it.
- `carbonet-nano/*` (6 enrolled repos) — will switch from file-copy bootstrap to
  the plugin enable-block in Phase 4.
