# Next-session handoff

_Written: 2026-07-23 15:30 PT_

## Branch & state
- Branch: `main` (worktree: N/A)
- Uncommitted: clean (handoff files committed by this skill)
- Behind/ahead of origin: **up to date** (`226ae2a`)

## What shipped this session
Stack repo (`get-lade/claude-code-stack`), all on `main`, CI green:
- `cd0470a` — ci: unstick main — regen stale capability-registry + guard mirror-sync
- `5bfedf6` — fix(mirror-sync): scope App token to the tenant mirror repo
- `6426790` — harden(pack-vendor): repo_root containment guard on apply_project_claude_fragment (handoff #6)
- `1e32dbd` + `b3d0f71` — feat + harden: **Job B** — tenant pack `standards/` distribution as PRs (ADR-034 §5, handoff #5)
- `226ae2a` — docs(contract): supersede the same-process rule per engine ADR-065

Engine repo (`get-lade/Architect-for-Claude-Code`):
- **PR #436 MERGED** (squash, CI green) — ADR-065 + plan for the Cloudflare Wrangler deploy + secret binding (handoff #4, design only, no code yet).

Provisioned this session (GitHub, not code): mirror-sync GitHub App is **live** — see memory `mirror-sync-app-provisioned`. Mirror `CarboNet-Nano/claude-code-stack-core` auto-syncs on every push.

## What's blocked & why
- Nothing blocked. #4 is decided (ADR-065), ready to build next session.

## Exact next steps
Build #4 in the **engine** repo (`Architect-for-Claude-Code`) per ADR-065, in slices:
1. **npm package `@lade/secret-binder`** — wrap the stack's `scripts/lib/secret-binder.sh` under a `package.json`, publish to a private registry (GitHub Packages). Delivery mechanism the owner chose.
2. **`Publisher` port** — `platform/packages/contracts/src/publisher.ts` (mirror `datastore.ts`'s `DataStoreProvisioner`); token never a field; injectable `PublishRunner`.
3. **`wrangler-publisher.ts` adapter** — `platform/packages/data/src/` (mirror `neon-provisioner.ts`). **Two** separate `execFile` children: bind (token in env) → `wrangler deploy` (scrubbed env), invoked only if bind resolved. `tenant.json` → `0600` temp file in an isolated per-run workspace, unlinked after bind.
4. **KV/zone/DNS provisioning** — IN scope; must run before the deploy exec (else `wrangler deploy` fails transactionally on declared bindings).
5. **Saga Step 6** — wire into `platform/apps/web/lib/wizard-materialize.ts::runNeonProvisionSaga` behind a feature flag.
6. **CF ops** — scoped deploy token (`Secrets Store: Read`, not Global API Key) as a lade-held engine secret `<TENANT>_API_TOKEN`.
- First test (slice 1): missing-secret → bind fails → deploy never runs (mirror stack `tests/test-publish-ordering.sh` case 2). Cross-family review the credential-path slices (ADR-025).

## Gotchas
- Sandbox pushes to `main` and `gh pr create` DO work here (used both this session). Engine repo has branch protection: PR needed `run-tests`/`typecheck`/`e2e`/`acceptance` green before merge.
- Deploy image (slice 3) must carry `bash`, `jq`, `curl`, `wrangler` with a `command -v` preflight that fails hard BEFORE any token is sourced (ADR-065 D1 risk).
- Stack contract §5 is now SUPERSEDED (two-exec, not one process) — don't re-implement the old same-process design.
- Mirror-sync: adding a NEW top-level installable-core dir means updating BOTH `.gitattributes` AND `mirror-scrub.sh` allowlist, or install-completeness/scrub breaks.
- Cross-org GitHub App gotcha (onboarding another tenant): App must be PUBLIC to install cross-org; token scoped via `owner`/`repositories`; org ruleset needs the App as a bypass actor for force-push. See memory `mirror-sync-app-provisioned`.

## Cross-repo references
- **Engine `Architect-for-Claude-Code`** — owns #4 build (ADR-065 merged, PR #436) and #3 Job B's engine-side counterpart (Job B itself shipped in the stack repo this session).
- Stack `scripts/lib/secret-binder.sh` is the source the engine's `@lade/secret-binder` package wraps.
- Contract doc `docs/plans/2026-07-22-pipedream-wrangler-secret-binding-contract.md` (stack) — §5 amended.

## Team this session
- Used: Explore ×1 (engine map), reviewer/OpenAI ×1 (Job B), red-team/Gemini ×1 (Job B), architect ×1 (D1–D3), architecture-critic/Gemini ×1 (design critique).
- Benched: none flagged — Job B (distribution + write-PAT) got full cross-family review; #4 design got architect → Gemini critic → owner. ADR-025 satisfied.
- Cross-family earned its keep: Gemini overturned 3 architect calls (two-exec token leak, npm vs vendor-dir, KV/zone deferral); OpenAI found 5 Job B blockers.

## Follow-ups (non-blocking)
- **review-router is content-blind** — grades high-stakes by file PATH not diff content; missed Job B's force-push/PAT gate (both reviewers flagged). Fix candidate for ADR-025. See memory `review-router-content-blind`.
