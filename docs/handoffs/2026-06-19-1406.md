# Next-session handoff

_Written: 2026-06-19 14:06 PT_

## Resume prompt
> "ADR-018 Setup Dashboard is shipped + live. Pick next: (a) smoke-test /stack-config live in a real repo, (b) harden the accepted residual TOCTOU in native-settings-edit, (c) fix/replace the tier-blocked Gemini CLI, or (d) new work."

## Branch & state
- Branch: `main` (worktree: N/A) — up to date with origin.
- Uncommitted: clean.
- ADR-017 (PR #33) AND ADR-018 (PR #34) both merged to `main` this session.

## What shipped this session
- `85d2f5f` — feat: ADR-018 Setup Dashboard + native-settings-edit writer (#34)
- `45daa63` — chore: gitignore __pycache__ + drop a committed .pyc (#35)
- `a60116a` — docs: ADR-018 build summary + cross-repo deployment guide
- `67ff83e` — chore: Tier 4 bump + ADR-017 capability recommender (#33)
- (earlier) ADR-017 capability recommender pushed + merged at session start.

## What ADR-018 delivered (all on `main`, installed in `~/.claude` via update.sh)
- `/stack-config` — read-first Setup Dashboard (stack + native settings), 4 ops, row-state tags, secret redaction.
- `/config` — `model-invocable:false` forwarding stub (built-in `/config` reclaimed).
- `native-settings-edit` — the ONLY native `settings.json` writer; python3 executable implementing all 12 contract items; `user-invocable:false`.
- `tests/test-native-settings-edit.sh` (47 cases), tier-2 manifest entries, registry regen (60 caps).
- Guide: `docs/ADR-018-deploy-guide.md`.

## Verification done (do NOT redo unless code changes)
4 cross-model passes via **Codex** (Gemini CLI is tier-blocked — see Gotchas). red-team (66 probes) → reviewer (found `--repo-root` scope bypass, fixed) → security-auditor (found symlink-follow **RCE** on `.tmp`/`.lock`, fixed: mkstemp + `O_NOFOLLOW` + in-lock TOCTOU re-check) → re-audit CONVERGED. Full suite 10/10; contract suite 47/47; registry `--check` green.

## Exact next steps
1. **Live smoke test** (any tier-≥2 repo): `/stack-config` → show-current (confirm native rows render + secrets redacted); → change-one → Model (confirm it runs `native-settings-edit --dry-run` and diffs before writing). The dogfood repo is tier 4 — test here.
2. **Deploy to other machines/repos:** `cd claude-code-stack && git pull && ./scripts/update.sh --tier=<N>` per machine. Per repo: just need `stack_tier >= 2`. Full steps in `docs/ADR-018-deploy-guide.md`.
3. **(Optional) Accepted residuals** in `native_settings_edit.py` (documented in ADR-018 Consequences): MED residual TOCTOU + LOW orphan-lock — both require a same-UID concurrent attacker (out of the hostile-repo threat model). A real fix needs `openat`/`O_DIRECTORY` fd-chaining (non-stdlib, ctypes). Only do if the threat model changes.
4. **(Optional) Gemini CLI** — restore the cross-model red-team's Gemini lane (see Gotchas).

## Gotchas
- **Gemini CLI is hard-blocked:** `IneligibleTierError — Gemini Code Assist for individuals no longer supported; migrate to the Antigravity suite`. gemini-cli 0.42.0 fails even with `GEMINI_API_KEY` set + `selectedAuthType` configured. Saved to memory (`gemini-cli-tier-blocked.md`). Until fixed, route red-team / architecture-critic via **Codex** (works fine) — NOT Claude-only.
- **Codex CLI works** (`codex exec --skip-git-repo-check "…"`); ~30–60s/call, occasionally exhausts budget re-reading large files mid-review (relay partial findings — not a failure).
- Sandbox CAN push `main` + open/merge PRs this session (the maintainer granted push+merge). Confirm that still holds next session before pushing to `main`.
- `native-settings-edit` refuses ALL writes in cloud (read-only by design) and refuses user-global writes without `--scope user --confirm-global`.

## Team this session
- Used (via Agent tool): reviewer (Codex) ×1, security-auditor (Codex) ×2, general-purpose (Codex red-team) ×1.
- Benched: architect, implementer (work was author-direct on a fully-specified Accepted ADR + a security-critical single artifact), validator (the 47-case suite + red-team-run-suite covered it), tester (suite written author-direct), product-critic, designer, documenter, scribe, plus all tier-3/4 roles. Acceptable — ADR-018 was pre-designed/red-teamed last session; this session was implement + adversarial-verify.
